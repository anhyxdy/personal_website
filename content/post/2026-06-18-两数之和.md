+++
title = "LeetCode 两数之和（Two Sum）"
date = 2026-06-18T10:00:00+08:00
draft = false
tags = ["LeetCode", "数组", "哈希表"]
categories = ["算法"]
description = "经典的 Two Sum 问题，三种解法思路"
+++

## 题目

给定一个整数数组和一个目标值，找出数组中和为目标值的两个数的下标。

## 解法一：暴力枚举

两层循环逐个检查，时间复杂度 O(n²)。

## 解法二：哈希表

遍历一遍，用哈希表记录已访问的值，O(n) 搞定。

```python
def two_sum(nums, target):
    seen = {}
    for i, num in enumerate(nums):
        complement = target - num
        if complement in seen:
            return [seen[complement], i]
        seen[num] = i
```
