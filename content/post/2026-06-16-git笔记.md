+++
title = "Git 常用命令速查笔记"
date = 2026-06-16T15:00:00+08:00
draft = false
tags = ["Git", "笔记", "技术"]
categories = ["后端"]
description = "整理了一些日常开发中最常用的 Git 命令，方便随时查阅"
+++

## 基础操作

```bash
# 初始化仓库
git init

# 克隆远程仓库
git clone https://github.com/user/repo.git

# 查看状态
git status

# 查看提交历史
git log --oneline --graph --all
```

## 分支管理

```bash
# 创建并切换到新分支
git checkout -b feature/new-feature

# 查看所有分支
git branch -a

# 删除本地分支
git branch -d old-branch

# 合并分支
git merge feature/new-feature
```

## 暂存与提交

```bash
# 添加文件到暂存区
git add .
git add src/app.ts    # 添加单个文件

# 提交
git commit -m "feat: add user login page"

# 修改上一次提交
git commit --amend
```

## 远程操作

```bash
# 推送到远程
git push origin main

# 拉取远程更新
git pull origin main

# 强制推送（谨慎使用！）
git push --force-with-lease origin main
```

## 撤销操作

```bash
# 撤销工作区修改
git checkout -- filename

# 撤销暂存区的修改
git reset HEAD filename

# 回退到某个版本
git reset --soft HEAD~1   # 保留修改
git reset --hard HEAD~1   # 丢弃修改
```

## 实用技巧

```bash
# 储藏当前修改
git stash
git stash pop

# Cherry-pick 某个提交
git cherry-pick <commit-hash>

# 查看某个文件的修改历史
git log -p filename
```

> 善用 `git status` 和 `git log`，它们是日常使用最频繁的两个命令。

记不住的命令随时回来看 👆
